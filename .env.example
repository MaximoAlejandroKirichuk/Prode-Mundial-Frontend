# Backend Registration API
# Base URL of the hosted backend that handles registration + Mercado Pago checkout.
# The frontend fetches the active tournament at runtime from GET /api/tournaments/active
# and posts registrations to POST /api/registrations.
# No frontend-owned tournament ID is needed — the backend is the source of truth.
PUBLIC_BACKEND_REGISTRATION_URL=http://localhost:5000
